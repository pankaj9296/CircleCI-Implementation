machine:
  php:
    version: 5.6.17
  environment: 
    ENVIRONMENT: development 
    CIRCLE_ENV: test
deployment:
  production:
    branch: master
    commands:
      - sudo git status
      - sudo git add .
      - sudo git commit -m 'circleci'
      - sudo git push origin
steps:
  add-ssh-keys:
    fingerprints:
      - "ab:fb:17:31:25:d9:84:2f:48:7b:10:7e:24:f4:6f:8c"
