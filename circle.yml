machine:
  environment: 
    ENVIRONMENT: development 
    CIRCLE_ENV: test
    DATABASE1_HOST: localhost
    DATABASE1_NAME: circle_test
    DATABASE1_USER: ubuntu
    DATABASE1_PASS: 
    NGROK_TOKEN: R5uVC1ye65hKoHHyZWLe_W6faFsUrkpKKuzWzQ1zj
    GHOST_API_KEY: fbef579153262df7ecf4bfaab92f87ad3d563265
    GHOST_SUITE_ID: 58ec6458c0b8a24d292f0e74
dependencies:
  post:
    - sudo cp ~/test/circle_build_virtual_host /etc/apache2/sites-available
    - sudo a2ensite circle_build_virtual_host
    - sudo touch /etc/apache2/conf-available/fqdn.conf
    - echo "ServerName localhost" | sudo tee /etc/apache2/conf-available/fqdn.conf
    - sudo a2enconf fqdn
    - sudo service apache2 restart

